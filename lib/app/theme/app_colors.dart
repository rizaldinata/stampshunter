import 'package:flutter/material.dart';

class AppColors {
  // Primary (Light theme)
  static const primary = Color(0xFF8B4513);
  static const primaryLight = Color(0xFFA0522D);
  static const primaryDark = Color(0xFF6B3410);
  static const primarySurface = Color(0xFFFFF8F0);

  // Primary (Dark theme - Amber)
  static const primaryDarkTheme = Color(0xFFFFB300);
  static const onPrimaryDarkTheme = Color(0xFF000000);

  // Secondary
  static const secondary = Color(0xFF2E7D32);
  static const secondaryLight = Color(0xFF4CAF50);
  static const accent = Color(0xFFD4A574);
  static const accentLight = Color(0xFFE8C9A0);

  // Neutral (Light)
  static const background = Color(0xFFFAFAFA);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceVariant = Color(0xFFF5F0EB);
  static const border = Color(0xFFE0D6CC);
  static const borderLight = Color(0xFFF0EBE5);

  // Neutral (Dark)
  static const surfaceDark = Color(0xFF0A0A0A);
  static const onSurfaceDark = Color(0xFFFFFFFF);
  static const onSurfaceVariantDark = Color(0xFFA3A3A3);
  static const outlineDark = Color(0xFF262626);
  static const primaryContainerDark = Color(0xFF332400);

  // Text
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF666666);
  static const textTertiary = Color(0xFF999999);
  static const textOnPrimary = Color(0xFFFFFFFF);

  // Semantic
  static const error = Color(0xFFD32F2F);
  static const errorLight = Color(0xFFFFEBEE);
  static const success = Color(0xFF2E7D32);
  static const successLight = Color(0xFFE8F5E9);
  static const warning = Color(0xFFF57C00);
  static const warningLight = Color(0xFFFFF3E0);
  static const info = Color(0xFF1976D2);
  static const infoLight = Color(0xFFE3F2FD);
}
